#include <stdio.h>
#include <stdlib.h>
extern char *transform_to_json(char *token, char *payload);
extern char *transform_to_csv(char *token, char *payload);
// extern char *transform_to_xml(char *Sender, char *Payload);
extern char *transform_to_html(char *token, char *payload);
